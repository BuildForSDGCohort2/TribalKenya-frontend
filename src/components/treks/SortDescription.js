import React from 'react';

const SortDescription = ({ sortDes }) => {
  return (
    <div className="sort-description center c-cream pt-3">
        <p className="medium-text bold overpass small-caps">{sortDes}</p>
    </div>
  );
};

export default SortDescription;
